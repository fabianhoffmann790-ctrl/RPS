export function ExportImportPage() {
  return (
    <section className="rounded-2xl bg-white p-6 shadow-sm">
      <h2 className="text-2xl font-bold">Export / Import</h2>
      <p className="mt-3 text-lg">
        Bereich für Datenexport und -import. In Milestone 1 ist dies bewusst als Platzhalter vorbereitet.
      </p>
      <div className="mt-4 rounded-2xl bg-slate-100 p-6 text-lg">
        <p>Hier folgen in späteren Milestones JSON-Export, JSON-Import und Validierungsfeedback.</p>
      </div>
    </section>
  );
}
